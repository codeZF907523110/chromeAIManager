- 所有的代码都要符合vue3的规范
- 改动前先写个详细的方案，然后审查自己的方案
- 根据方案文档进行改动
- 要优先看业界内的优秀的方案，不要自己发明方案，也不要自己猜测
- 如果有新增的方案应该先改方案文档，然后再改
- 改动不允许影响到其它的正常功能
- lint、ts、格式化错误，不能有错误
- 代码能简化就简化，不能增加复杂度，代码要注意模块化、可维护性、可读性、可扩展性等
- 每个函数都要加注释，写清楚当前函数的功能、参数、返回值、异常等
- 改动后要测试一下，确保没有问题
- 改动后要更新方案文档和技术文档
- 改动完之后要仔细审查代码，要确保和方案一致
- 现在还没上线，这是第一版，所以代码要干净一些，不能有冗余的代码、重复的代码、错误的代码、没用的代码等
- 改动完之后要审查代码逻辑，要审查以下内容：
  - lint、ts、格式化错误，不能有错误
  - 梳理代码链路，确保代码逻辑没有问题
  - 绝对不能影响其它的正常逻辑
  - 检查代码的可维护性、可读性、可扩展性等
  - 检查代码是否有重复的代码、冗余的代码、错误的代码、没用的代码等
